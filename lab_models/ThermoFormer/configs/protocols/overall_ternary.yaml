extends: ../training/supervised.yaml
name: c1_supervised_ternary
protocol:
  registered_splits: [overall_ternary]
  seeds: [0, 1, 2, 3, 4]
  evaluation_partition: test
runtime:
  output_dir: experiments/vle/generalization/training_records/comparisons/ternary_only
  results_file: experiments/vle/comparison/study_records/ternary_only/results.md
