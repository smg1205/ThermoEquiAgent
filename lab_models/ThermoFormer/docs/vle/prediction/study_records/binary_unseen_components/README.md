# Binary-only unseen-component generalization

Held-out components are selected from the binary VLE pool. Training and
validation contain no held-out component, and the final C1 model uses the same
registered three-stage training method as the main experiment.
