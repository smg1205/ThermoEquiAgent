# Run

```powershell
conda run -n ggnn39 python scripts/visualization/plot_overall_phase_diagrams.py
```

The case study uses the formal seed-0 test partition. It does not combine cases across random seeds because each seed has a different system-disjoint test assignment.
