# Final C1 overall binary/ternary evaluation

`config.yaml`, `run.md`, and `results.md` describe the final C1 model with
validation-gated fugacity fine-tuning. Registered checkpoints and evaluation artifacts are listed in `models/README.md`.
