# 校验

校验检查以下各项：温度 / 压力为正值、归一化组成在界内、物料衡算、相平衡残差、求解器收敛性、参数适用范围以及稳定性信息。结果分为 `passed`、`warning`、`failed` 三档。

**不收敛与物理性失败不能被 LLM 覆盖。** 共沸点的判定要求存在内部组成满足 `x 约等于 y`，而不是仅仅在图上出现交叉。

对 TP 闪蒸而言，相态输出与进料闭合为必需项。当前的 K 值相态分类**不构成切平面距离（TPD）稳定性证明**，因此在具备经验证的 TPD 实现之前，稳定性检查始终保留为显式警告。
