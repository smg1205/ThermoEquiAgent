# Frontend

The Next.js workbench separates conversation, task state, and result evidence. The task panel keeps
diagnostics visible; chart, table, model, parameter, validation, and run tabs show the calculation
from different engineering perspectives. Conditions are editable and a rerun creates a new run.
